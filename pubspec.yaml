name: personal_finance_app_beta
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  awesome_snackbar_content: ^0.1.1
  cloud_firestore: ^4.8.3
  firebase_auth: ^4.7.1
  firebase_core: ^2.15.0
  flutter:
    sdk: flutter
  freezed: ^2.4.1
  intl: ^0.18.1
  json_serializable: ^6.7.1
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  text_scroll: ^0.2.0

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

